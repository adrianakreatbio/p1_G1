name: g1_app
description: Simple cross-platform gel quantification app with IMX415 camera
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  camera: ^0.11.3
  camera_windows: ^0.2.1+6
  path_provider: ^2.1.2
  path: ^1.8.3
  permission_handler: ^12.0.1
  open_filex: ^4.4.0
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
